* { 
	color: var(panel3[text1]); 
	font-size: var(font[text_size])px;
	font-family: var(font[family]);
}

/*******************************************
* CONTENT
*******************************************/
#mainFrame{
	border: 0px solid var(panel3[border]);
	background:  var(panel3[background]);
	border-radius: 5px;
}

/* HEADER                                  */
/*-----------------------------------------*/
#logo{
	background:  var(titlebar[background]);
	
}

#mainHeader{
	background:  var(titlebar[background]);
	color: var(titlebar[text3]);
	border-bottom: 2px solid var(titlebar[border]);
}

#mainHeader QLabel{
	background:  var(titlebar[background]);
	 
}

#appTitle{
	font-weight: bold;
	color: var(titlebar[text1]);
}

#appDescription{
	color: var(titlebar[text2]);
}
/*******************************************/
/* WINDOWACTIONS TOPICONS                  */
/*******************************************/
#windowicons{
	border: none
}

#windowicons QLabel{
	background:  var(titlebar[background]); 
}

#windowicons QPushButton{
	background: transparent;
	border: none;
}
#windowicons QPushButton#restorewindow:hover,
#windowicons QPushButton#maximizewindow:hover,
#windowicons QPushButton#minimizewindow:hover{ 
	border: none;
	background: var(titlebar[hover]);
}

#windowicons QPushButton#closewindow:hover{ 
	background: red;
}

QLabel#appLogo{
	background:  var(titlebar[background]);
}
/*******************************************/
/* FOOTER                                  */
/*******************************************/
#mainFooter QFrame{
	border: none;
}

#mainFooter{
	background:  var(footerbar[background]);
	color: var(footerbar[text2]);
}

#footerActions QPushButton,
#mainFooter QPushButton{ 
	border: none;
	background: var(footerbar[background]);
	color: var(footerbar[text1]);
}

#footerActions QPushButton:hover{
    background: var(footerbar[hover]);
	color: var(footerbar[text1]);
}

#mainFooter QLabel{
	color: var(footerbar[text2]);
}


/*******************************************/
/* PANELS                                  */
/*******************************************/

#controllerPanel1{
	background: var(dividers[background]);
    border-top: none;  
}
#controllerPanel2{
 	background: var(dividers[background]);
	border-top: none;
}
#controllerPanel3{
	background: var(dividers[background]);
    border-top: none;  
}
#controllerPanel4{
	background: var(dividers[background]);
    border-top: none;  
}
#controllerPanel5, #controllerPanel6{
	background: var(dividers[background]);
    border-top: none;  
}

#controllerPanel1{
    border-left: 1px solid var(dividers[border]);	
}
#controllerPanel2{
    border-left: 1px solid var(dividers[border]);	
}
#controllerPanel3{
    border-left: 1px solid var(dividers[border]);	
}
#controllerPanel4{
    border-left: 1px solid var(dividers[border]);	
}
#controllerPanel5{
    border-left: 1px solid var(dividers[border]);	
}

#controllerPanel5{
    border-top: 1px solid var(dividers[border]);	
}

#controllerPanel1 QPushButton, 
#controllerPanel2 QPushButton, 
#controllerPanel3 QPushButton,
#controllerPanel4 QPushButton, 
#controllerPanel5 QPushButton, 
#controllerPanel6 QPushButton{
	background: transparent;
	border: none;
}

#controllerPanel1 QPushButton:hover, 
#controllerPanel2 QPushButton:hover, 
#controllerPanel3 QPushButton:hover,
#controllerPanel4 QPushButton:hover, 
#controllerPanel5 QPushButton:hover, 
#controllerPanel6 QPushButton:hover{
	background: var(dividers[hover]);
}

LabelHorizontal, LabelVertical, #controllerPanel5 QLabel{
	font-weight: bold;
	color: var(dividers[text1]);
	font-size: 16px;
	padding-left: 20px;
}
#panel1{
	background: var(panel1[background]);
    color: var(panel1[text2]);
}
#panel2{
	background: var(panel2[background]);
   color: var(panel2[text2]);
}
#panel3{
	background: var(panel3[background]);
    color: var(panel3[text2]);
}
#panel4{
	background: var(panel4[background]);
   color: var(panel4[text2]);
}
#panel5, #panel6{
	background: var(panel5[background]);
   color: var(panel5[text2]);
}

#controllerPanel5{
    border-bottom: 1px solid var(dividers[border]);
}
#controllerPanel1, #controllerPanel2{
    border-left: 1px solid var(dividers[border]);
}

/*
#controllerPanel3, #controllerPanel4, #controllerPanel5, #controllerPanel6{
    border-right: 1px solid var(dividers[border]);
}
*/

#panel6{
    border-right: 1px solid var(panel5[border]);
}

#controllerPanel1 QFrame,
#controllerPanel2 QFrame,
#controllerPanel3 QFrame,
#controllerPanel4 QFrame,
#controllerPanel5 QFrame{
	border: none;
}

/*******************************************/
/* BUTTONS                                 */
/*******************************************/
QPushButton{
	padding: 5px;
    background: var(panel3[header]);
	border: 1px solid var(panel3[border]);
	color: var(panel3[text1]);
}

 QPushButton:hover{
    background: var(panel3[hover]);
	border: 1px solid var(panel3[border]);
	color: var(panel3[text2]);
}


/*******************************************/
/* SCROLLBAR                               */
/*******************************************/
QScrollBar {
    background: var(panel3[border]);
    width: 8px;   
}

QScrollBar::handle {
    background:  var(panel3[header]);
}

QScrollBar::add-line:vertical, 
QScrollBar::add-line:horizontal  {
    border: none;
    background: none;
}

QScrollBar::sub-line:vertical, 
QScrollBar::sub-line:horizontal  {
    border: none;
    background: none;
  }

QScrollBar::handle:hover:vertical, 
QScrollBar::handle:hover:horizontal {
    /*background: var(panel3[background]);*/
    border-radius: 3px;
}

/*******************************************/
/* TREEVIEW                                */
/*******************************************/
QTreeView, QTreeWidget{
    alternate-background-color: var(panel3[bgalternate]);
	background:  var(panel3[background]);
	border: none;
}

#panel1 QTreeWidget{
	background:  var(panel1[background]);
}

QTreeView::Item{
    height:20px;
}

#panel3 QHeaderView::section, 
#panel3 QHeaderWidget::section {
    color: var(panel3[text1]);
	font-weight: bold;
    background: var(panel3[header]);
	padding: 2px;	
	border: none;
	border-bottom: 1px solid var(panel3[border]);
}
#panel1 QHeaderView::section, 
#panel1 QHeaderWidget::section {
    color: var(panel1[text1]);
	font-weight: bold;
    background: var(panel1[header]);
	padding: 2px;	
	border: none;
	border-bottom: 1px solid var(panel1[border]);
}
#panel4 QHeaderView::section, 
#panel4 QHeaderWidget::section {
    color: var(panel4[text1]);
	font-weight: bold;
    background: var(panel4[header]);
	padding: 2px;	
	border: none;
	border-bottom: 1px solid var(panel4[border]);
}
/*******************************************/
/* TOOLBOX                            */
/*******************************************/


/*******************************************/
/* TOOLTIP                            */
/*******************************************/
QToolTip { 
	background-color: var(panel3[background]); 
	color: var(panel3[text1]); 
	border: 1px solid var(panel3[border]); 
	border-radius: 3px;
}

/*******************************************/
/* LINEEDIT                            */
/*******************************************/
/*
QLineEdit{
    background-color: var(panel3[background]);
    color: #dce1ec;
}
*/
/*
QLineEdit {
	background-color: var(panel3[bgalternate]);
	border: 1px solid transparent;
	padding-left: 10px;
    padding-right: 10px;
	selection-background-color: var(panel3[hover]);
    color: var(panel3[text3]);
}
QLineEdit:focus {
	border: 1px solid var(panel3[border]);
    background-color: var(panel3[bgalternate]);
	color: var(panel3[text2]);
}
*/
/*******************************************/
/* ToolBox                                 */
/*******************************************/

/*******************************************/
/* TABS                                    */
/*******************************************/
QTabBar::tab {
	background: var(panel3[header]);
	border: 1px solid var(panel3[border]);
	padding: 5px 20px;
	/*
	font-weight: bold;
	*/
	/*min-width: 100px;*/
  }

#panel3 QTabBar::scroller{
	width: 0px;
}

QTabBar::tab:selected {
	background: var(panel3[hover]);
	margin-bottom: -1px;
	color: var(panel3[text2]);
	border-bottom: 2px solid var(panel3[header]); 
}

QTabBar::tab:!selected:hover {
	background: var(panel3[hover]);
	color: var(panel3[text2]);
}

QTabWidget::pane {
	/*border: 1px solid var(panel3[background]);*/
	top:0px;
	background: transparent;
	border: 1px solid var(panel3[border]);
}

QTabBar::tab:right,
QTabBar::tab:left {
	padding: 20px 5px;
}


/*******************************************/
/* SCROLLAREA                            */
/*******************************************/
QScrollArea, #scrollArea2, #scrollArea1, #scrollArea2 QFrame, #scrollArea1 QFrame{
	border: none
}

#scrollAreaWidgetContents{
    background: var(panel3[background]);
    border: 1px solid var(panel3[background]);
}
#scrollAreaWidgetContents2{
    background: var(panel3[background]);
    border: 1px solid var(panel3[background]);
}
/*******************************************/
/* TABLE                            */
/*******************************************/
QTableView, QTableWidget{
	alternate-background-color: var(panel3[bgalternate]);
    border: 1px solid var(panel3[border]);
    color: var(panel3[text1]);
	background:  var(panel3[background]);
}

QTableCornerButton::section{
    background-color: var(panel3[header]);
    border-bottom: 1px solid #222;
    border-right: 1px solid #222;
    color: #8a95aa;
}

#contentMenu QLabel{
	border-bottom: 1px solid var(panel1[border]);
	font-weight: bold;
    background: var(panel1[header]);
	padding-right: 30px;	
	font-style: italic;
	color: var(panel1[text1]);
	font-size: var(font[title_size])px;
	
}

#pageHeader QLabel{
	border-bottom: 1px solid var(panel3[border]);
	font-weight: bold;
    background: var(panel3[header]);
	padding-right: 30px;	
	font-style: italic;
	color: var(panel3[text1]);
	font-size: var(font[title_size])px;
	
}

#settingsHeader QLabel{
	border-bottom: 1px solid var(panel2[border]);
	font-weight: bold;
    background: var(panel2[header]);
	padding-right: 30px;	
	font-style: italic;
	color: var(panel2[text1]);
	font-size: var(font[title_size])px;
}

/*******************************************/
/* NAVIGATION MENU                         */
/*******************************************/
#contentMenu QTreeWidget{
	font-weight: bold;
	show-decoration-selected: 1;
}

#contentMenu QTreeWidget::Item{
    height:30px;
	font-weight: bold;
	border-bottom:0px solid;
	/*padding-top: 5px;*/
	padding-left: 10px;
	margin-right: 10px;
	color: var(panel1[text2]);
}

#contentMenu QTreeWidget::Item:selected{
	color: var(panel1[text3]);	

}

#contentMenu QTreeWidget::item:hover {
    color: var(panel1[text3]);
	border: 0px solid;
}

#contentMenu QTreeWidget::branch::vertical{
	background: var(panel1[background]);
}

#contentMenu QTreeWidget::Item:!has-children:selected{
	/*border-left:5px solid var(panel3[border]);*/
	color: var(panel1[text3]);
}


#contentMenu QTreeWidget::branch  {
    border-image: none;
    image: none;
	
	background: var(panel1[background]);
}

/*
#contentMenu QTreeWidget::item {
	padding: 0;
  }
*/	
/*******************************************/
/* LISTVIEW                            */
/*******************************************/

QTreeView, 
QListView {
	show-decoration-selected: 1;
}
QTreeView::item:selected, 
QListView::item:selected {
	  background: var(panel3[hover]);
	  color: var(panel3[text3]);
	  border: 0px solid var(panel3[background])
}
QTreeView::item:selected:!active, 
QListView::item:selected:!active {
	  background: transparent;
	  border: 0px solid;
}
QTreeView::item:selected:active,
QListView::item:selected:active {
	 background: transparent;
	 border: 0px solid;
}
/*******************************************/
/* PROGRESS                            */
/*******************************************/
QProgressBar{
	background: var(panel3[bgalternate]);
	color: var(panel3[text3]);
}

QProgressBar{
    border: 2px solid var(footerbar[border]);
    border-radius: 5px;
    text-align: center
}

QProgressBar::chunk {
    background-color: var(footerbar[background]);
    width: 10px;
    margin: 1px;
}

#mainFooter QProgressBar::chunk {
    background-color: red;
}

